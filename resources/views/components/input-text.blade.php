<div class="flex flex-col">
    <label for="{{ $id }}" class="font-medium text-gray-700">{{ $label }}</label>
    <input {{ $attributes }} name="{{ $id }}" id="{{ $id }}" class="input w-full mt-3"  autocomplete="off">
</div>
